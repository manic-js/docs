---
title: useTheme
description: Hook returning theme preference, resolved light/dark, setTheme, toggle, and boolean helpers — requires ThemeProvider.
---

# `useTheme()`

Returns the active **`ThemeContext`** value. **Throws** if called outside **`ThemeProvider`**.

```tsx twoslash
// @errors: 2322
import { useTheme } from 'manicjs';

export function Banner() {
  const { resolvedTheme, isDark, setTheme } = useTheme();
  return (
    <header data-theme={resolvedTheme}>
      <button type="button" onClick={() => setTheme('system')}>
        Use system
      </button>
    </header>
  );
}
```

---

## Signature

```ts
function useTheme(): ThemeContextValue;
```

<TypeTable
  type={{
    theme: {
      type: "'light' | 'dark' | 'system'",
      description: 'Stored user preference.',
    },
    resolvedTheme: {
      type: "'light' | 'dark'",
      description: 'Effective theme after resolving system preference.',
    },
    setTheme: {
      type: '(theme: Theme) => void',
      description: 'Persist preference and update DOM.',
    },
    toggle: {
      type: '() => void',
      description: 'Flip between light and dark based on resolvedTheme.',
    },
    isDark: {
      type: 'boolean',
      description: 'True when resolvedTheme === "dark".',
    },
    isLight: {
      type: 'boolean',
      description: 'True when resolvedTheme === "light".',
    },
  }}
/>

---

## Imports

```ts
import { useTheme } from 'manicjs';
import { useTheme } from 'manicjs/theme';
```

---

## See also

- [ThemeProvider](/docs/api/theme/theme-provider)
- [ThemeToggle](/docs/api/theme/theme-toggle)
- [Theme overview](/docs/api/theme)
